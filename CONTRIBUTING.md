# Contributing to Agent Copilot

Read `AGENTS.md` before editing. Keep changes scoped, follow existing Rust and
native macOS patterns, and update the relevant current contract when behavior
changes.

After implementation, run once:

```sh
pnpm check:macos
```

Pull requests should explain the outcome, scope, and known limitations.
