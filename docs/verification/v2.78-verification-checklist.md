# V2.78 Verification Checklist

Status: completed on 2026-06-15 after coordinator verification.

V2.78 is completed because the protocol/docs/gate parity implementation landed, the coordinator reviewed the multi-agent output, ran the required deterministic gates, and recorded the command results below.

V2.78 scope: protocol / validation gate parity after V2.77. The slice aligns the service-protocol documentation with the current Rust service method surface, documents the local and CI-equivalent validation gate story, and preserves V2.46-V2.64 verification history without inventing missing evidence.

V2.78 is protocol / validation gate parity only: documentation, gate, and governance parity without product-scope expansion. It does not rename protocol methods, expand protocol payloads, add provider default calls, add write/apply paths, execute scripts, read credentials, sync cloud data, emit telemetry, add public release automation, or weaken real-local evidence standards.

## Boundary To Close

- Protocol source of truth: `crates/service/src/protocol.rs` `SUPPORTED_METHODS` remains the canonical list for service method names exposed through `service.status.supported_methods`.
- Method count: the current mainline source list contains 93 methods. Any docs verifier should compare names against `SUPPORTED_METHODS` rather than a hand-maintained historical count alone.
- Protocol docs: `docs/service-protocol.md` must describe the supported method surface, compatibility rules, contract fixture expectations, and V2.41-V2.67 AI/read-only surfaces without treating examples as additional methods.
- Validation parity: local and CI-equivalent gates must tell the same story for Rust formatting/tests/clippy, service protocol fixtures, supported-method dispatch coverage, Swift tests, validation blockers, screenshot artifacts, privacy checks, and versioned docs closeout checks.
- V2.46-V2.64 verification history: keep historical closeout evidence as recorded, including explicit Computer Use/window/tool-layer blockers and privacy reasons for not committing live screenshots. Do not backfill screenshots, PIDs, or command results that were not actually captured.
- Planning wording: V2.78 is complete as a docs/gate slice. V2.79-V2.83 have since completed as separate follow-ups.

## Non-goals

- No protocol method rename.
- No protocol payload expansion.
- No protocol version bump.
- No provider default calls or background network calls.
- No write/apply path, skill file mutation, agent config mutation, triage mutation, snapshot creation, or rollback behavior change.
- No script execution or script preview behavior change.
- No credential reads or persistence changes.
- No raw prompt, raw response, raw trace, secret, or unredacted path persistence.
- No cloud sync, telemetry, public distribution, signing, notarization, DMG, or ZIP automation.
- No replacement of unlocked manual visual review where real-local UI validation is required.

## Coordinator Evidence Checklist

- [x] Implementation evidence for the V2.78 docs/gate parity slice is present and reviewed.
- [x] `SUPPORTED_METHODS` in `crates/service/src/protocol.rs` is confirmed as the protocol method source of truth.
- [x] Service protocol docs are checked against the current supported method names.
- [x] The docs/method drift verifier, `pnpm verify:service-protocol-drift`, is run and recorded.
- [x] The V2.78 closeout verifier, `pnpm verify:v2.78-docs`, is run and recorded.
- [x] The aggregate gate parity lane, `pnpm verify:gate-parity`, is run and recorded.
- [x] CI or CI-equivalent local gate parity is recorded, including Swift, Rust, validation blocker, screenshot artifact, privacy, and versioned docs checks.
- [x] V2.46-V2.64 verification-history governance is documented without inventing missing evidence.
- [x] V2.82 and V2.83 have since completed after V2.78 closeout, and V2.78 did not mark later versions completed at its own closeout.
- [x] Safety boundaries are confirmed: no protocol rename, payload expansion, provider default call, write/apply path, script execution, credential read, cloud sync, or telemetry.

## Verification Commands

Coordinator command results:

```sh
cargo fmt --all -- --check                           # passed
cargo test -p skills-copilot-service --lib supported_methods_have_dispatch_coverage  # passed
cargo test -p skills-copilot-service --lib service_protocol_fixtures_decode          # passed
pnpm verify:service-protocol-drift                   # passed
pnpm verify:v2.78-docs                               # passed
pnpm verify:gate-parity                              # passed
cargo test --workspace                               # passed via pnpm check:macos
cargo clippy --workspace --all-targets --all-features -- -D warnings  # passed via pnpm check:macos
swift test --package-path apps/macos                 # passed via pnpm check:macos
pnpm verify:validation-blockers                      # passed via pnpm verify:gate-parity
pnpm verify:screenshot-artifacts                     # passed via pnpm verify:gate-parity and pnpm check:macos
pnpm verify:v2.73-docs                               # passed via pnpm verify:gate-parity
pnpm verify:v2.74-docs                               # passed via pnpm verify:gate-parity
pnpm verify:v2.75-docs                               # passed via pnpm verify:gate-parity
pnpm verify:v2.76-docs                               # passed via pnpm verify:gate-parity
pnpm verify:v2.77-docs                               # passed via pnpm verify:gate-parity
pnpm check:macos                                     # passed
pnpm check:privacy                                   # passed
git diff --check                                     # passed
```

## V2.46-V2.64 Verification-history Governance

V2.46-V2.64 predate the V2.72 canonical blocker tooling and V2.73-V2.77 completed screenshot-evidence pattern. Their historical closeouts remain valid only as recorded history, not as permission to relax current validation standards.

- V2.46-V2.62: preserve the recorded focused Rust/Swift/protocol checks, `pnpm check:macos`, `pnpm check:privacy`, real-local launch attempts, and explicit Computer Use/window blockers. Do not add retroactive app-window screenshots or PIDs.
- V2.63: preserve the recorded real UI verification and the later exact-path
  retry blocker separately. The uncommitted live screenshot remains unaccepted
  because it exposed local paths.
- V2.64: preserve the recorded provider observability checks, fixture smoke evidence, and real-local window/tool-layer blocker. Do not convert fixture smoke into completed real-local UI evidence.
- Historical `cgWindowNotFound`, `remoteConnection`, `timeoutReached`, no-AX-window, locked-session, black/flat/transparent/invalid capture, Screen Recording permission, stale-bundle, and unknown tool-layer cases should be mapped to canonical blocker language when summarized, but the original historical record should not be rewritten as if V2.72 tooling existed then.
- Fixture smoke may support build/service health. It cannot replace blocked real-local Computer Use or manual visual review for current user-visible UI closeouts.
- Privacy rules still apply to historical and current evidence: do not commit screenshots or docs that expose local paths, usernames, app-data paths, credentials, tokens, raw prompts, raw provider responses, or raw traces.

## Closeout Notes

- Coordinator run date: 2026-06-15.
- Coordinator command log: local command results recorded above.
- V2.78-specific docs verifier: `pnpm verify:v2.78-docs` passed.
- Real-local UI evidence: not required because V2.78 changed protocol docs, fixtures, deterministic verifiers, CI/local gate wiring, and historical validation governance only. No native user-visible UI surface was added or changed. V2.79 and V2.80 were completed in later user-visible follow-up closeouts with fresh unlocked real-local Computer Use evidence.
- Final status decision: completed.

## Safety Boundary

V2.78 must remain a protocol/docs/gate parity slice:

- No protocol rename or payload expansion.
- No provider default calls.
- No write/apply path.
- No skill file or agent config mutation.
- No triage mutation.
- No snapshot or rollback behavior change.
- No script execution.
- No credential reads.
- No raw prompt/response/trace persistence.
- No cloud sync.
- No telemetry.
- No weakening of V2.72-V2.77 validation evidence standards.
