# V2.98 Verification Checklist

Status: completed

## Scope

V2.98 closes Automatic local session discovery and session-derived skill usage
summaries for the current Agent Copilot Sessions/Skills workflow.

- [x] `session.previewLocalSessions` auto-discovers supported Claude Code, Codex, opencode, and Pi local session stores when no explicit authorized roots are supplied.
- [x] The response returns redacted `session_rows`, bounded filterable
  `content_items`, and session-derived `skill_usage_rows`.
- [x] Skill usage counts are derived from explicit local invocation markers such
  as `/skill`, `/skill:<name>`, and `skill:<name>`, not ordinary skill-name
  mentions.
- [x] Tool calls and skill calls remain separate counters from user/agent/thinking
  message totals.
- [x] Hermes/OpenClaw session parsing remains deferred until their session-store
  formats have confirmed evidence.
- [x] No credential read, credential write, credential export, or credential
  handling expansion was added.
- [x] No provider default call, write path, script execution, credential access,
  raw transcript persistence, cloud sync, telemetry, signing, packaging, DMG,
  ZIP, notarization, or release automation was added.

## Code And Data

- [x] `crates/service/src/service_support_helpers.rs` implements automatic local session
  roots, bounded session file scanning, redacted content item extraction, and
  `skill_usage_rows`.
- [x] `crates/service/src/tests/local_session_preview.rs` covers supported auto-discovery,
  project scoping, content item classification, explicit skill invocation
  detection, and opencode message/part enrichment boundaries.
- [x] `apps/macos/Sources/SkillsCopilot/Models/LocalSessionPreview.swift` and
  `apps/macos/Sources/SkillsCopilot/Stores/SkillStore.swift` decode and surface
  the V2.98 session rows, content items, and usage summaries.
- [x] Service protocol fixtures were refreshed for
  `session.previewLocalSessions` with `generated_by=local-v2.98`.

## Validation

- [x] `cargo fmt --all -- --check` passed.
- [x] `cargo test --workspace` passed.
- [x] `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  passed.
- [x] `swift test --package-path apps/macos` passed.
- [x] `swift build --package-path apps/macos` passed.
- [x] `pnpm verify:service-protocol-drift` passed with 93 documented methods,
  93 supported methods, 93 dispatch arms, 93 status fixture methods, 94 request
  fixture cases, and 94 response fixture cases.
- [x] `pnpm verify:macos-ui-layout` passed.
- [x] `pnpm verify:gate-parity` passed, including `pnpm verify:v2.98-docs`.
- [x] `pnpm check:macos` passed end to end with fixture-data smoke and
  app-window-only screenshot capture to a temporary `/tmp` artifact.
- [x] `pnpm check:privacy` passed.
- [x] `git diff --check` passed.

## Safety Decision

Final status decision: completed.

V2.98 is read-only local evidence discovery. It does not persist raw transcript
content, raw prompts, raw responses, secrets, or unredacted paths, and it does
not mutate skill files, agent config, snapshots, triage, cleanup state, provider
state, credentials, cloud data, telemetry, or distribution artifacts.
