# V2.94 Verification Checklist

Status: completed

## Scope

V2.94 closes the Pi install and compatibility-root writes slice without adding
new service methods.

- [x] Pi now scans native `~/.pi/agent/skills` / project `.pi/skills` roots
  plus `.agents/skills` compatibility roots.
- [x] Pi `.agents/skills` roots use `RootSource::Compatibility`.
- [x] Pi compatibility-root toggles update Pi settings, not `.agents` skill
  files.
- [x] Project compatibility toggles use project-bound settings snapshots and
  block explicit untrusted markers; 2026-06-19 real local validation confirmed
  project `.pi/skills` loads without a positive `project.trusted` marker.
- [x] Tool-global install supports native-root install to
  `~/.pi/agent/skills` and project `.pi/skills`.
- [x] Pi package install/remove remains blocked.
- [x] `.agents/skills` direct skill-file installs remain blocked.
- [x] No provider calls, provider default calls, script execution, credential
  reads, cloud sync, telemetry, hidden write path, package install/remove, or
  uncontrolled network fetch was added.

## Source Evidence

- Pi Skills docs:
  <https://pi.dev/docs/latest/skills>
- Pi Settings docs:
  <https://pi.dev/docs/latest/settings>
- Pi Security docs:
  <https://pi.dev/docs/latest/security>
- Pi Packages docs:
  <https://pi.dev/docs/latest/packages>

V2.94 uses the documented native and `.agents/skills` roots for local scanning,
the documented settings/trust boundary for guarded toggles, and intentionally
does not implement `pi install` / `pi remove` package management.

## Code And Data

- [x] `crates/adapters/src/pi/mod.rs` adds global/project `.agents/skills`
  compatibility roots.
- [x] `crates/scanner/src/lib.rs` keeps Pi scanning directory-rooted and
  filters direct root `.md` noise.
- [x] `crates/commands/src/lib.rs` supports guarded Pi native/compat toggles,
  native-root install targets, and explicit package/direct-compat install
  blockers.
- [x] `fixtures/service-protocol/adapter.listCapabilities.response.json`,
  `service.status.response.json`, and `app.stateSnapshot.response.json` expose
  the V2.94 Pi capability matrix.
- [x] Fixture smoke creates disposable Pi native and `.agents/skills`
  compatibility roots plus trusted project `.pi/settings.json`.
- [x] Pi capability coverage is verified by service protocol fixtures and
  disposable smoke roots; the historical writable evidence harness is no
  longer part of the current app surface.

## Validation

- [x] `cargo fmt --all -- --check` passed.
- [x] `cargo test -p skills-copilot-adapters pi --lib` passed.
- [x] `cargo test -p skills-copilot-scanner pi --lib` passed.
- [x] `cargo test -p skills-copilot-commands pi --lib` passed.
- [x] `cargo test -p skills-copilot-service service_protocol_fixtures_decode --lib` passed.
- [x] `pnpm verify:service-protocol-drift` passed.
- [x] `pnpm verify:v2.94-docs` passed.
- [x] `pnpm verify:gate-parity` passed.
- [x] `./script/build_and_run.sh --verify` built the Rust service and Swift app,
  launched `dist/AgentCopilot.app`, and confirmed a visible app window.
- [x] `pnpm check:macos` passed, including app launch, fixture smoke, app-window
  capture, and screenshot artifact verification.
- [x] `pnpm smoke:macos-app -- --fixture-data --capture-window` passed and
  refreshed `docs/ui-artifacts/native-macos-shell/completed.png`.
- [x] `./script/capture_app_window.sh dist/AgentCopilot.app docs/ui-artifacts/v2.94-pi-install-compat-writes/completed.png`
  captured a dedicated V2.94 app-window screenshot.
- [x] `pnpm verify:screenshot-artifacts docs/ui-artifacts/v2.94-pi-install-compat-writes`
  passed.
- [x] `pnpm check:privacy` passed.
- [x] `git diff --check` passed.

## Evidence

- UI evidence directory:
  `docs/ui-artifacts/v2.94-pi-install-compat-writes`.
- App-window evidence:
  `docs/ui-artifacts/v2.94-pi-install-compat-writes/completed.png`.
- Evidence notes:
  `docs/ui-artifacts/v2.94-pi-install-compat-writes/verification.md`.

## Safety Decision

Final status decision: completed.

V2.94 resolves Pi install and compatibility-root writes by allowing only
guarded Pi settings toggles for native / `.agents` compatibility instances and
native-root direct installs. It does not enable package install/remove,
`.agents` direct skill-file installs, provider calls, script execution,
credential reads, raw prompt/response/trace persistence, cloud sync,
telemetry, uncontrolled network fetch, hidden write/apply paths, signing,
notarization, DMG, or ZIP work.
