# V2.76 Verification Checklist

Status: completed on 2026-06-15.

V2.76 scope: make Task Cockpit progress understandable while existing local evidence calls run. Completion proves readiness/routing/agent/provider/session staged feedback, partial rows, elapsed time, timeout/fallback/blocked states, and real-local Computer Use evidence.

V2.76 is a UI feedback, status, and validation slice over existing Task Cockpit behavior. It does not add new provider default calls, write/apply paths, script execution, credential reads, cloud sync, telemetry, raw prompt persistence, hidden task state, scanner/catalog fact changes, or new analysis/provider service semantics by default.

## Completed Boundary

- Readiness stage: visible stage status for task readiness work while the cockpit operation is preparing and after fallback/partial completion.
- Routing stage: visible stage status for route confidence work while the cockpit operation is preparing and after fallback/partial completion.
- Cross-agent stage: visible stage status for cross-agent task readiness work, including active and partial states.
- Review stage: visible stage status for read-only action review and batch-check context, including partial and skipped states.
- Provider stage: visible provider observability context from existing app-local redacted metadata only; no provider/network requests are triggered by default.
- Session stage: visible local session context from existing app-local deterministic evidence only; no raw transcript persistence or provider request is introduced.
- Elapsed time: bounded, human-readable elapsed-time feedback is shown so `Preparing...` cannot be mistaken for a deadlock.
- Partial rows: useful local rows remain visible when a stage times out, falls back, or is skipped.
- Timeout/fallback/blocked states: the UI distinguishes bounded fallback, blocker count, timeout state, skipped stages, and retry.
- Real-local evidence: completion used unlocked Computer Use against the current `<repo>/dist/SkillsCopilot.app`, plus app-window-only screenshot evidence.

## Completed Evidence Checklist

- [x] Implemented staged Task Cockpit feedback for readiness/routing/agent/provider/session phases.
- [x] Implemented partial row rendering so successful stage output remains visible when another stage times out, falls back, or is skipped.
- [x] Implemented elapsed-time display for long-running Task Cockpit generation.
- [x] Implemented timeout/fallback/blocked state copy that distinguishes bounded degradation from failure.
- [x] Preserved V2.75 task input behavior: AX-settable multiline input, exact nonblank task text, whitespace-only submit blocking, and explicit Build submit.
- [x] Preserved V2.73 bounded recovery behavior: cancel/retry, stale completion protection, scan/detail limits, and fallback diagnostics.
- [x] Proved provider feedback uses existing observability metadata only and does not send provider/network requests by default.
- [x] Proved session feedback uses existing app-local deterministic session evidence only and does not persist raw transcripts.
- [x] Added focused Swift model/UI tests for staged feedback, partial rows, elapsed time, timeout/fallback/blocked states, and main-window accessibility IDs.
- [x] Documented that V2.76 is UI/status-only over existing service semantics; service protocol fixture decoding only checks optional aggregation metadata.
- [x] Ran `pnpm verify:v2.76-docs`.
- [x] Ran `pnpm check:privacy`.
- [x] Ran `pnpm check:macos`.
- [x] Ran `pnpm verify:screenshot-artifacts`.
- [x] Ran `git diff --check`.
- [x] Validated with unlocked real-local Computer Use against the current `<repo>/dist/SkillsCopilot.app`.
- [x] Captured app-window-only screenshot evidence after real-local validation.
- [x] Updated README.md, AGENTS.md, docs/plans/development-tasks.md, docs/plans/roadmap.md, this checklist, and the V2 docs verifier from planned to completed after evidence existed.

## Real-local Computer Use Evidence

The completed real-local pass identified the app being validated without relying on ambiguous app-name lookup alone:

- App bundle: `<repo>/dist/SkillsCopilot.app`
- Bundle id: `dev.skills-copilot.native`
- Running PID: `39728`
- Main window accessibility id: `skills-copilot.main-window`
- Task input accessibility id: `skills-copilot.task-cockpit.input`
- Task input status accessibility id: `skills-copilot.task-cockpit.input.status`
- Stage feedback accessibility id: `skills-copilot.task-cockpit.stage-progress`
- Captured app window id: `36736`
- Screenshot evidence: `docs/ui-artifacts/v2.76-progressive-cockpit-feedback/completed.png`

The real-local pass entered a nonblank Chinese/multiline task through the V2.75 AX-settable input, observed `Ready for explicit submit.`, clicked Build explicitly, observed staged feedback before final output, confirmed partial rows remained visible in fallback/blocked state, and verified no provider request/write/execute/credential/cloud/telemetry path was introduced.

During build, Computer Use read back the Task Cockpit status container `skills-copilot.task-cockpit.status` with `正在准备本地 Cockpit... 超时前 5/15 秒。`, the stage progress container `skills-copilot.task-cockpit.stage-progress`, `耗时：5 秒`, queued/active readiness/routing/agent/provider/session stage labels, and progress bar value `0.3333333333333333`.

After bounded fallback, Computer Use read back the Retry button, result container `skills-copilot.task-cockpit.result`, the stage progress container `skills-copilot.task-cockpit.stage-progress`, `耗时：6 秒`, `Fallback / 部分`, `10 个阻塞项`, `已超时`, partial statuses for readiness/routing/Provider 观测 stages, and skipped statuses for non-applicable review stages.

## Stage Feedback Matrix

| Stage | Completed visible evidence | Safety gate |
| --- | --- | --- |
| Readiness | Preparing, partial, fallback, timed-out, and blocker-count status for task readiness rows | Local deterministic evidence only |
| Routing | Preparing, partial, fallback, timed-out, and blocker-count status for route candidates | Local deterministic evidence only |
| Cross-agent | Active and partial cross-agent readiness status in the staged feedback strip | Local deterministic evidence only |
| Review | Partial action-review status and skipped batch-check status when the bounded budget is reached | Read-only preview context only |
| Provider | Existing provider observability status rows with no automatic send | No new provider default calls |
| Session | Existing local session context and skipped status when no bounded result is available | No raw transcript persistence |
| Overall Cockpit | Elapsed time, partial rows, timeout/fallback/blocked message, Retry, and stable result read-back | No hidden task state or background job semantics |

## Commands

```sh
swift test --package-path apps/macos --filter 'TaskCockpitModelTests|MainWindowModelTests|TaskInputModelTests|SkillStoreTests'
pnpm verify:macos-ui-layout
./script/build_and_run.sh --verify
pnpm verify:screenshot-artifacts
pnpm check:privacy
pnpm verify:v2.76-docs
pnpm check:macos
git diff --check
```

## Safety Boundary

V2.76 remains local, user-triggered, and read-only by default:

- No new provider default calls.
- No write/apply path.
- No skill file or agent config mutation.
- No triage mutation.
- No snapshot creation or rollback.
- No script execution.
- No credential reads.
- No raw prompt persistence.
- No raw response JSON persistence.
- No raw transcript or raw trace persistence.
- No cloud sync.
- No telemetry.
- No hidden task state.
- No new analysis/provider service semantics by default.

## Canonical Blockers

V2.76 completion did not rely on a blocker. If a future validation retry cannot complete, record exactly one primary canonical blocker:

- `locked-session`
- `window-not-found`
- `no-ax-window`
- `computer-use-timeout`
- `remote-connection`
- `activation-failed`
- `black-capture`
- `flat-capture`
- `transparent-capture`
- `invalid-capture`
- `screen-recording-permission`
- `stale-bundle`
- `tool-layer-unknown`

Fixture smoke, no-capture launch success, and app-window screenshots are supporting evidence only. They cannot replace unlocked real-local Computer Use for Progressive Cockpit feedback.
