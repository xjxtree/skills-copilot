# macOS Development

Build without launching:

```sh
pnpm build:macos
```

Build and run:

```sh
pnpm dev:macos
```

After implementation, run once:

```sh
pnpm check:macos
```

Manual UI inspection is optional. Agents may use Computer Use or any other
suitable method without a prescribed interaction or evidence format.
